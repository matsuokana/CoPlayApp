# CoPlay アプリ 仕様書

## 概要

小学生向けのスマートフォン対応WEBアプリ。  
楽しく音を出したり、画面を光らせたりできる2つの機能を提供する。

---

## 対象ユーザー・動作環境

| 項目 | 内容 |
|------|------|
| 対象ユーザー | 小学生（6〜12歳） |
| 動作端末 | スマートフォン（iOS / Android） |
| アクセス方法 | ブラウザからWEBアプリにアクセス |
| 対応ブラウザ | Safari（iOS）、Chrome（Android） |
| 通信 | インターネット接続が必要（音の録音・登録時） |

---

## UI 基本方針

- **テーマ：子供向けクラシックコンサート**
  - 深みのある紺・金・えんじなどを基調とした上品な配色
  - ベルベットのカーテン・コンサートホールを連想させるデザイン
  - 楽器アイコンは自作SVGイラストで統一する
- 文字は大きく、読みやすいフォントを使用する（明朝体やセリフ系が望ましい）
- ボタンは大きくタップしやすいサイズにする（最小 60px × 60px）
- アイコンや絵で機能を直感的に伝える（文字だけに頼らない）
- 金色の枠線や装飾でコンサートホールの雰囲気を演出する
- 操作は最小限のタップで完結できるようにする
- エラーメッセージは子供にわかりやすい言葉で表示する

---

## 機能1：おもちゃ楽器サウンドボード

### 概要

画面上のボタンを押すと、おもちゃの楽器の音が鳴る。  
スマホを振ると選択中の音が鳴る。  
ユーザーが自分で録音した音を登録して、オリジナルのサウンドボードを作れる。

### 画面構成

```
┌─────────────────────────────┐
│ ◀もどる  🎵 おとをならそう！ │
├─────────────────────────────┤
│  ┌──────┐ ┌──────┐ ┌──────┐ │
│  │[SVG] │ │[SVG] │ │[SVG] │ │
│  │カッコウ│ │ウズラ │ │ナイチ│ │
│  └──────┘ └──────┘ └──────┘ │
│     ...（3列グリッド）       │
├─────────────────────────────┤
│     ＋ おとをついか          │
└─────────────────────────────┘
```

### デフォルト搭載音源（12種）

アイコンはすべて自作SVG（`icons/` フォルダ）。  
音はすべてWeb Audio APIで合成する。

| ボタン名 | 音の種類 | SVGファイル |
|----------|----------|------------|
| カッコウ笛 | カッコウの下降2音（ミ→ド） | cuckoo.svg |
| ウズラ笛 | 3連符風の上昇音 | quail.svg |
| ナイチンゲール笛 | トリル風の速い音形 | nightingale.svg |
| マラカス | マラカス振り音 | maraca.svg |
| おもちゃのラッパ | おもちゃの交響曲風3和音 | toyrumpet.svg |
| おもちゃのたいこ | おもちゃのドラム打撃音 | toydrum.svg |
| カスタネット | カスタネット打ち音（2連打） | castanet.svg |
| ぼよよーん | コミカルなバネ音 | boing.svg |
| シンバル | シンバルノイズ音 | cymbal.svg |
| タンバリン | 打撃音＋ジングル時間差 | tambourine.svg |
| トライアングル | 高い正弦波の長い余韻 | triangle.svg |
| ラチェット | 短いクリック連打音 | ratchet.svg |

### 振り検知との連携

- サウンドボード画面でスマホを振ると、選択中の音が鳴る
- 音のタイルをタップすると選択状態になる（枠線で強調表示）

### 音の録音・登録機能

#### 録音フロー

1. 「＋おとをついか」ボタンをタップする
2. 楽器アイコン（絵文字）を選ぶ
3. ボタンの名前（ひらがな・カタカナ・漢字・最大8文字）を入力する
4. 「🔴 ろくおんスタート」ボタンをタップして録音を開始する
5. 音を出す（最大10秒）
6. 「⏹ とめる」ボタンをタップして録音を終了する
7. 「▶ さいせい」ボタンで録音した音を確認できる
8. 「🎉 このおとでけってい！」ボタンを押すと登録完了
9. サウンドボードに新しいボタンが追加される

#### 録音仕様

| 項目 | 仕様 |
|------|------|
| 最大録音時間 | 10秒 |
| 使用API | MediaRecorder API |
| 保存形式 | WebM（Opus） |
| 保存先 | IndexedDB（ブラウザローカル） |
| 最大追加登録数 | 5音（デフォルト12音 ＋ 追加5音 = 最大17音） |

#### 録音した音の管理

- 長押しで「✏️ なまえをかえる」「🗑 けす」メニューが表示される
- デフォルト音源は削除・名前変更不可
- 削除前に確認ダイアログを表示する（「ほんとうにけしますか？」）

### マイク権限

- 録音開始時にブラウザのマイク許可を求める
- 許可されなかった場合、わかりやすいメッセージで案内する

---

## 機能2：スマホを振って画面を光らせよう

### 概要

好きな色を選んでスマホを振ると、画面全体がその色で光る。  
振れない場合はタップボタンでも光らせられる。

### 画面構成

```
┌─────────────────────────────┐
│ ◀もどる ✨ ふって光らせよう！│
├─────────────────────────────┤
│                             │
│   いろをえらんでね           │
│                             │
│  🔴  🟠  🟡  🟢             │
│  あか だいだい き みどり     │
│  🔵  🟣  💗  ⬜             │
│  あお むらさき ピンク しろ   │
│                             │
│  スマホをふると光るよ！      │
│                             │
│  ┌─────────────────────┐   │
│  │  タップして光らせる  │   │
│  └─────────────────────┘   │
│                             │
└─────────────────────────────┘
```

### 色の選択

| 色名 | カラーコード |
|------|-------------|
| あか | `#FF4444` |
| だいだい | `#FF8800` |
| き | `#FFDD00` |
| みどり | `#44CC44` |
| あお | `#4488FF` |
| むらさき | `#AA44FF` |
| ピンク | `#FF44AA` |
| しろ | `#FFFFFF` |

- 各色は大きな丸いボタンで表示する
- 選択中の色はボタンが強調表示（金色枠線・拡大）される

### ゆれ検知

| 項目 | 仕様 |
|------|------|
| 使用API | DeviceMotion API |
| 検知しきい値 | 加速度の合計が 20 m/s² 以上 |
| 振り認識後の動作 | 画面全体が選択色で光り、0.4秒後に元の画面に戻る |
| 連続振り | 0.5秒のクールダウン後、再度検知可能 |

### 発光演出

- 画面全体を選択色で覆うオーバーレイを表示する
- フェードイン（0.1秒）→ 点灯（0.3秒）→ フェードアウト（0.1秒）のアニメーション
- 効果音なし

### センサー権限

- iOS 13以降のみ `DeviceMotion API` の許可が必要（Android・PCは不要）
- **取得タイミング**：ホーム画面のボタン（「おとをならそう！」「ふって光らせよう！」）をタップした際に自動でiOSのシステムダイアログを表示する
- **制約**：iOSは1ページセッションにつきダイアログを1回しか表示しない。一度拒否するとリロードするまで再表示されない
- **後から許可する方法**：発光画面に「📳 スマホふる を ゆるす」ボタンを常設する（iOS未許可時のみ表示）。リロード後にこのボタンを押すと再度ダイアログが出る
- 許可されなかった場合でも画面に遷移し、「タップして光らせる」ボタンで代替操作できる

---

## 画面遷移

```
起動
  │
  ▼
ホーム画面
  ├── [🎵 おとをならそう] ──→ （iOS: センサー許可ダイアログ）──→ サウンドボード画面
  │                              ├── タップ → 音を鳴らす・選択
  │                              ├── 振る  → 選択中の音を鳴らす
  │                              ├── [＋ついか] ──→ 録音画面
  │                              └── [長押し]   ──→ 編集メニュー
  │
  └── [✨ ふって光らせよう] ──→ （iOS: センサー許可ダイアログ）──→ カラー選択・発光画面
                                   ├── 振る  → 選択色で画面が光る
                                   ├── タップ → 選択色で画面が光る
                                   └── [📳 スマホふる を ゆるす]（iOS未許可時のみ表示）
```

---

## ホーム画面

```
┌─────────────────────────────┐
│                             │
│     🎻 CoPlay               │
│   こどものえんそうかい       │
│                             │
│  ┌─────────────────────┐   │
│  │  🎵 おとをならそう！ │   │
│  └─────────────────────┘   │
│                             │
│  ┌─────────────────────┐   │
│  │ ✨ ふって光らせよう！│   │
│  └─────────────────────┘   │
│                             │
└─────────────────────────────┘
```

---

## データ管理

| データ | 保存先 | 備考 |
|--------|--------|------|
| ユーザー登録音源（音声データ） | IndexedDB | ブラウザローカル保存 |
| ボタン設定（名前・アイコン） | IndexedDB | 音声データと同一レコード |

- サーバーへのデータ送信は行わない（プライバシー配慮）
- ブラウザデータ削除時にユーザーデータも消える

---

## 技術スタック

| 分類 | 採用技術 |
|------|----------|
| フロントエンド | HTML / CSS / JavaScript（Vanilla） |
| 音声合成 | Web Audio API |
| 音声録音 | MediaRecorder API |
| モーション検知 | DeviceMotion API |
| データ保存 | IndexedDB |
| アイコン | 自作SVG（`icons/` フォルダ） |
| スタイリング | CSS（スマホ縦持ち最適化） |
| ホスティング | GitHub Pages（静的サイト配信） |

---

## 非機能要件

| 項目 | 要件 |
|------|------|
| レスポンシブ | スマートフォン縦持ち（375px〜430px幅）を主対象 |
| パフォーマンス | 音の再生遅延 100ms 以内 |
| アクセシビリティ | ボタンに aria-label を付与する |
| スケール | 2000人同時アクセス対応（全処理がクライアント完結のため） |
| セキュリティ | マイク音声はローカル保存のみ、外部送信なし |

---

## 今後の拡張案（スコープ外）

- 録音した音をリズムに合わせて自動再生するシーケンサー機能
- 友達とリアルタイムで一緒に演奏できるオンライン機能
- カラーグラデーションや虹色など発光パターンの追加
- 音に合わせて自動で色が変わるモード
