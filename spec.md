# CoPlay アプリ 仕様書

## 概要

小学生向けのスマートフォン対応WEBアプリ。  
楽しく音を出したり、画面を光らせたりできる2つの機能を提供する。

---

## 対象ユーザー・動作環境

| 項目 | 内容 |
|------|------|
| 対象ユーザー | 小学生（6〜12歳） |
| 動作端末 | スマートフォン（iOS / Android） |
| アクセス方法 | ブラウザからWEBアプリにアクセス |
| 対応ブラウザ | Safari（iOS）、Chrome（Android） |
| 通信 | インターネット接続が必要（音の録音・登録時） |

---

## UI 基本方針

- **テーマ：子供向けクラシックコンサート**
  - 深みのある紺・金・えんじなどを基調とした上品な配色
  - ベルベットのカーテン・コンサートホールを連想させるデザイン
  - 楽器アイコンはクラシカルなイラスト調で統一する
- 文字は大きく、読みやすいフォントを使用する（明朝体やセリフ系が望ましい）
- ボタンは大きくタップしやすいサイズにする（最小 60px × 60px）
- アイコンや絵で機能を直感的に伝える（文字だけに頼らない）
- 金色の枠線や装飾でコンサートホールの雰囲気を演出する
- 操作は最小限のタップで完結できるようにする
- エラーメッセージは子供にわかりやすい言葉で表示する

---

## 機能1：おもちゃ楽器サウンドボード

### 概要

画面上のボタンを押すと、おもちゃの楽器の音が鳴る。  
ユーザーが自分で録音した音を登録して、オリジナルのサウンドボードを作れる。

### 画面構成

```
┌─────────────────────────────┐
│  🎵 おとをならそう！         │
├─────────────────────────────┤
│  ┌──────┐  ┌──────┐         │
│  │ 🥁   │  │ 🎸   │         │
│  │ たいこ│  │ギター│         │
│  └──────┘  └──────┘         │
│  ┌──────┐  ┌──────┐         │
│  │ 🎺   │  │ ➕   │         │
│  │トランペ│  │ついか│         │
│  └──────┘  └──────┘         │
└─────────────────────────────┘
```

### デフォルト搭載音源

以下の音を初期状態で用意する：

| ボタン名 | 音の種類 |
|----------|----------|
| たいこ | ドラム打撃音 |
| ギター | ギターかき鳴らし音 |
| トランペット | トランペット音 |
| ピアノ | ピアノ単音 |
| マラカス | マラカス振り音 |
| かね | ベル・鈴の音 |
| バイオリン | バイオリン弓弾き音 |
| ホイッスル | ホイッスル吹き音 |
| カスタネット | カスタネット打ち音 |
| ぼよよーん | コミカルなバネ音 |
| シンバル | 小さなシンバル音 |
| タンバリン | タンバリン打ち・振り音 |
| おもちゃのたいこ | おもちゃのドラム打撃音 |
| すず | 鈴のシャラシャラ音 |

### 音の録音・登録機能

#### 録音フロー

1. 「＋ついか」ボタンをタップする
2. 楽器アイコン（絵文字）を選ぶ画面が表示される
3. ボタンの名前（ひらがな・カタカナ・漢字）を入力する
4. 「ろくおんスタート」ボタンをタップして録音を開始する
5. 音を出す（最大10秒）
6. 「とめる」ボタンをタップして録音を終了する
7. 「さいせい」ボタンで録音した音を確認できる
8. 「このおとでけってい！」ボタンを押すと登録完了
9. サウンドボードに新しいボタンが追加される

#### 録音仕様

| 項目 | 仕様 |
|------|------|
| 最大録音時間 | 10秒 |
| 使用API | Web Audio API / MediaRecorder API |
| 保存形式 | WebM（Opus）または MP3 |
| 保存先 | ブラウザのLocalStorage または IndexedDB |
| 最大登録数 | 18音（デフォルト13音 ＋ 追加5音） |

#### 録音した音の管理

- 長押しで「なまえをかえる」「けす」のメニューが表示される
- デフォルト音源は削除不可
- 削除前に確認ダイアログを表示する（「ほんとうにけしますか？」）

### マイク権限

- 初回録音時にブラウザのマイク許可を求める
- 許可されなかった場合、わかりやすいメッセージで再度案内する

---

## 機能2：スマホを振って画面を光らせよう

### 概要

好きな色を選んでスマホを振ると、画面全体がその色で光る。  
音楽に合わせて光らせたり、友達と違う色で光らせたりして遊べる。

### 画面構成

```
┌─────────────────────────────┐
│  ✨ ふって光らせよう！        │
├─────────────────────────────┤
│                             │
│   いろをえらんでね           │
│                             │
│  🔴  🟠  🟡  🟢  🔵  🟣    │
│  あか だいだい き みどり あお むらさき │
│                             │
│  ┌─────────────────────┐   │
│  │    ふってみよう！    │   │
│  └─────────────────────┘   │
│                             │
│  ← もどる                   │
└─────────────────────────────┘
```

### 色の選択

- 以下の8色をあらかじめ用意する：

| 色名 | カラーコード |
|------|-------------|
| あか | `#FF4444` |
| だいだい | `#FF8800` |
| き | `#FFDD00` |
| みどり | `#44CC44` |
| あお | `#4488FF` |
| むらさき | `#AA44FF` |
| ピンク | `#FF44AA` |
| しろ | `#FFFFFF` |

- 各色は大きな丸いボタンで表示する
- 選択中の色はボタンが強調表示（枠線・拡大）される

### ゆれ検知

| 項目 | 仕様 |
|------|------|
| 使用API | DeviceMotion API |
| 検知しきい値 | 加速度の合計が 20 m/s² 以上 |
| 振り認識後の動作 | 画面全体が選択色に切り替わり、0.5秒後に元の画面に戻る |
| 連続振り | 0.5秒のクールダウン後、再度検知可能 |

### 発光演出

- 画面全体を選択色で覆うオーバーレイを表示する
- フェードイン（0.1秒）→ 点灯（0.3秒）→ フェードアウト（0.1秒）のアニメーション
- 効果音（キラキラ音）を同時に再生する（オプション：ON/OFFを設定できる）

### センサー権限

- iOS 13以降ではDeviceMotion APIに許可が必要
- 初回起動時に許可を求めるボタンを表示する
- 許可されない環境では「このきのうはつかえません」と表示し、代替として「タップして光らせる」ボタンを提供する

---

## 画面遷移

```
起動
  │
  ▼
ホーム画面
  ├── [🎵 おとをならそう] ──→ サウンドボード画面
  │                              ├── [＋ついか] ──→ 録音画面
  │                              └── [長押し]   ──→ 編集メニュー
  │
  └── [✨ ふって光らせよう] ──→ カラー選択・発光画面
```

---

## ホーム画面

```
┌─────────────────────────────┐
│                             │
│     🎮 CoPlay               │
│   あそびのアプリ             │
│                             │
│  ┌─────────────────────┐   │
│  │   🎵 おとをならそう  │   │
│  └─────────────────────┘   │
│                             │
│  ┌─────────────────────┐   │
│  │ ✨ ふって光らせよう！│   │
│  └─────────────────────┘   │
│                             │
└─────────────────────────────┘
```

---

## データ管理

| データ | 保存先 | 備考 |
|--------|--------|------|
| ユーザー登録音源（音声データ） | IndexedDB | ブラウザローカル保存 |
| ボタン設定（名前・アイコン） | LocalStorage | JSON形式 |
| アプリ設定（効果音ON/OFF等） | LocalStorage | |

- サーバーへのデータ送信は行わない（プライバシー配慮）
- ブラウザデータ削除時にユーザーデータも消える旨を案内する

---

## 技術スタック（案）

| 分類 | 採用技術 |
|------|----------|
| フロントエンド | HTML / CSS / JavaScript（Vanilla または React） |
| 音声処理 | Web Audio API、MediaRecorder API |
| モーション検知 | DeviceMotion API |
| データ保存 | LocalStorage、IndexedDB |
| スタイリング | CSS（レスポンシブ対応、スマホ最適化） |
| ホスティング | GitHub Pages（静的サイト配信） |

---

## 非機能要件

| 項目 | 要件 |
|------|------|
| レスポンシブ | スマートフォン縦持ち（375px〜430px幅）を主対象 |
| パフォーマンス | 音の再生遅延 100ms 以内 |
| アクセシビリティ | ボタンに aria-label を付与する |
| オフライン | デフォルト音源はオフラインでも再生可能にする（ServiceWorker推奨） |
| セキュリティ | マイク音声はローカル保存のみ、外部送信なし |

---

## 今後の拡張案（スコープ外）

- 録音した音をリズムに合わせて自動再生するシーケンサー機能
- 友達とリアルタイムで一緒に演奏できるオンライン機能
- カラーグラデーションや虹色など発光パターンの追加
- 音に合わせて自動で色が変わるモード
